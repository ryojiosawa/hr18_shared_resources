/* Filter takes an array and returns an array of all elements that pass a truth test.
		
		Example: 
		filter([1,2,3,4], function(item) {return item % 2 === 0});
		>> [2,4]

*/
  var filter = function(array, test) {
  };


  /*
		Now write a function, Uniq, which takes an array, and returns a copy of this array 
		without duplicates.

		Example: uniq([1,1,2,2,3,3,4])
		>> [1,2,3,4]

		USE FILTER IN YOUR CODE
  */

  var uniq = function(array) {
  };