/*
	A Set is an abstract data structure used for testing membership. Implement a Set 
	with three methods, add, contains, and remove.

	1. use functional shared style- assume you have access to underbar
	2. elements should be unique (no duplicates)
*/

var makeSet = function(){
};

var setMethods = {};

setMethods.add = function(item){
};

setMethods.contains = function(item){
};

setMethods.remove = function(item){
};